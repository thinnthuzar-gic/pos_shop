# POS backend with spring


### Maven Parent overrides

I developed an E-Commerce backend system using Spring Boot. The system supports Admin and Client roles with JWT-based authentication and role-based authorization using Spring Security. Admin users can manage products and orders, while Clients can browse products and place orders. The application uses MySQL as the database and Hibernate for ORM. The project follows a layered architecture and RESTful API design principles.


